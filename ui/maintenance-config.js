/**
 * UI-999R｜ZDOS Maintenance Mode switch（v1.5 主產品線）
 * true  → 入口導向 maintenance.html
 * false → 恢復正常 index.html（Boot Splash → Login → Home）
 * 不涉及 Auth／Supabase／API／登入流程。
 */
window.MAINTENANCE_MODE = false;
